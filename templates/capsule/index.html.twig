{% extends 'base.html.twig' %}

{% block title %}🕰️ Capsule Temporelle{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('styles/app.css') }}">
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script type="module">
        import CapsuleManager from '{{ asset('capsule.js') }}';
    </script>
{% endblock %}

{% block body %}
<div class="min-h-screen bg-gradient-to-br from-purple-900 via-blue-900 to-indigo-900 flex items-center justify-center p-4">
    <div class="w-full max-w-2xl">
        {# Header avec titre #}
        <div class="text-center mb-8">
            <h1 class="text-5xl font-bold text-white mb-2">
                <span id="hourglass-icon" class="inline-block transition-transform duration-700 hover:rotate-360">⌛</span>
                Capsule Temporelle
            </h1>
            <p class="text-blue-200 text-lg">
                Écrivez un message pour le futur
            </p>
        </div>

        {# Container principal - géré dynamiquement par JavaScript #}
        <div id="capsule-container">
            {# Skeleton loader - état initial #}
            <div class="card backdrop-blur-lg bg-white/10 border-white/20 animate-pulse">
                <div class="card-header">
                    <div class="h-8 bg-white/20 rounded w-3/4 mb-2"></div>
                    <div class="h-4 bg-white/10 rounded w-1/2"></div>
                </div>
                <div class="card-content">
                    <div class="space-y-3">
                        <div class="h-4 bg-white/10 rounded"></div>
                        <div class="h-4 bg-white/10 rounded w-5/6"></div>
                        <div class="h-4 bg-white/10 rounded w-4/6"></div>
                    </div>
                </div>
                <div class="card-footer">
                    <div class="h-10 bg-white/20 rounded w-32"></div>
                </div>
            </div>
        </div>

        {# Footer #}
        <div class="text-center mt-8 text-blue-200 text-sm">
            <p>Challenge DevChallenges #2026-WEEK-01</p>
        </div>
    </div>
</div>
{% endblock %}
