{% extends 'base.html.twig' %}

{% block title %}🕰️ Capsule Temporelle{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('styles/app.css') }}">
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script type="module">
        import CapsuleManager from '{{ asset('capsule.js') }}';
    </script>
{% endblock %}

{% block body %}
<div class="h-screen bg-black flex flex-col">
    {# Contenu principal - flex-1 prend tout l'espace disponible #}
    <div class="flex-1 flex items-center justify-center px-4">
        <div class="w-full max-w-2xl">
            {# Header avec titre #}
            <div class="text-center mb-6">
                <h1 class="text-5xl font-bold mb-2 tracking-wider text-yellow-400" style="font-family: 'Courier New', monospace; text-transform: uppercase; letter-spacing: 0.2em; text-shadow: 0 0 20px rgba(251, 191, 36, 0.5), 0 0 40px rgba(251, 191, 36, 0.3);">
                    <span id="flux-icon" class="inline-block" style="font-size: 3rem;">⚡</span>
                    CAPSULE TEMPORELLE
                </h1>
                <p class="text-orange-400 text-sm tracking-wide" style="font-family: 'Courier New', monospace; text-transform: uppercase; letter-spacing: 0.15em; text-shadow: 0 0 10px rgba(251, 146, 60, 0.4);">
                    MACHINE A VOYAGER DANS LE TEMPS
                </p>
            </div>

            {# Container principal - géré dynamiquement par JavaScript #}
            <div id="capsule-container">
                {# Skeleton loader - état initial #}
                <div class="bg-brushed-metal p-6 animate-pulse">
                    <div class="text-center mb-4">
                        <div class="h-6 bg-zinc-700 rounded w-48 mx-auto"></div>
                    </div>
                    <div class="grid grid-cols-3 gap-3 mb-4">
                        <div class="led-box h-24"></div>
                        <div class="led-box h-24"></div>
                        <div class="led-box h-24"></div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    {# Footer en bas - flex-shrink-0 pour garder sa hauteur #}
    <footer class="flex-shrink-0 bg-zinc-900/80 border-t border-zinc-700 py-3 px-4">
        <p class="text-center text-yellow-400/70 hover:text-yellow-400 text-sm transition-colors">
            🚀 Challenge <a href="https://devchallenges.yoandev.co/challenge/2026-week-01/" target="_blank" rel="noopener noreferrer" class="underline hover:text-yellow-300">DevChallenges</a> #2026-WEEK-01
        </p>
    </footer>
</div>
{% endblock %}
